
@{
    ViewData["Title"] = "Home Page";

}



<main id="main">

    <section id="hero" class="hero d-flex align-items-center section-bg">
        <div class="container">
            <div class="row justify-content-between gy-5">
                @await Html.PartialAsync("~/Views/Home/Partial1.cshtml")
            </div>
        </div>
    </section>

    <section id="about" class="about">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial2.cshtml")
        </div>
    </section>

    <section id="why-us" class="why-us section-bg">
        <div class="container" data-aos="fade-up">
            <div class="row gy-4">
                @await Html.PartialAsync("~/Views/Home/Partial3.cshtml")
            </div>
        </div>
    </section>

    <section id="stats-counter" class="stats-counter">
        <div class="container" data-aos="zoom-out">
            <div class="row gy-4">
                @await Html.PartialAsync("~/Views/Home/Partial4.cshtml")
            </div>
        </div>
    </section>

    <section id="menu" class="menu">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial5.cshtml")
        </div>
    </section>

    <section id="events" class="events">
        <div class="container-fluid" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial6.cshtml")
        </div>
    </section>

    <section id="chefs" class="chefs section-bg">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial7.cshtml")
        </div>
    </section>

    <section id="book-a-table" class="book-a-table">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial8.cshtml")
        </div>
    </section>

   @*  <section id="gallery" class="gallery section-bg">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial9.cshtml")
        </div>
    </section> *@

    <section id="contact" class="contact">
        <div class="container" data-aos="fade-up">
            @await Html.PartialAsync("~/Views/Home/Partial10.cshtml")
        </div>
    </section>

    <div>
    </div>
</main>